# Example env file for local development
# Copy this to `.env` (for dev) or `.env.production` (for production) and fill the key.
# Do NOT commit real keys.

VITE_GEMINI_API_KEY=REPLACE_WITH_YOUR_KEY
# Optional alternative name used by config
GEMINI_API_KEY=REPLACE_WITH_YOUR_KEY

# Example:
# VITE_GEMINI_API_KEY=AIzaSy... (DO NOT COMMIT)
